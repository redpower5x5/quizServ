import 'dart:ui';

class UserState {
  final String name;
  final int id;
  final double totalPoints, roundPoints;
  final Color color;

  const UserState(this.id, this.name, this.totalPoints, this.roundPoints, this.color);


}