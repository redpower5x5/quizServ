name: quiz_app
description: Quolly quiz system.

publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.2+1

environment:
  sdk: '>=2.18.0-44.1.beta <3.0.0'

dependencies:
  flutter:
    sdk: flutter

  google_fonts: ^3.0.1
  auto_size_text: ^3.0.0
  flutter_svg: any
  provider: any
  rxdart: ^0.27.5
  socket_io_client: ^2.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^2.0.0


flutter:

  uses-material-design: false

  assets:
    - assets/